---
dataset: site
---

## Examples

### Site with an established name

This example shows that the site has its own stable reference. Its human-readable name, address and boundary help people identify it but do not replace that reference.

{{ example("named-site") }}
