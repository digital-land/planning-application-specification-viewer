# Tests

Tests of installed viewer behaviour belong here. Specification semantics remain tested in the specification package. The existing API smoke check is in `scripts/smoke_test_specification.py`.
